# 任务：合并 quality_gate.py 新旧逻辑 + 同步到 skill 仓库 + push 远端

## 前情

上一轮（BATCH 1）你重写了 `/root/repos/github-trending/scripts/quality_gate.py`，
加了 4 条新检查（code_wraps / bulk_line_piles / low_explanation_refs 等）。
但用户审后发现 2 个问题需要修：

### 问题 1：你把原版 6 条检查删掉了

老 skill 仓库 `/root/repos/skills/github-trending-monitor/scripts/quality_gate.py`
（sha=4e15c311）有这些检查，你的新版里全丢了：

| 原版检查 | 作用 | 丢了的后果 |
|---|---|---|
| layout shell 锚点 (`--ink/--panel/--star/--ease/.shell{/.nav{/.bg-stars`) | 防止页面 CSS 走样 | 新页面可能 layout 错乱 |
| 旧设计系统禁用 (`--bg-card/--bg-elev/--glow-amber`) | 防止视觉风格不一致 | 可能混入旧设计 |
| 主页面 H3 ≥ 10 | 防止章节太少 | 可能页面过于空洞 |
| 深度阅读入口 ≥ 2 | 强制链到子页 | 子页可能没引用 |
| 子页面与主页 CSS vars 一致 | 防止子页面样式走样 | 子页面可能脱节 |
| index.html 含项目卡片 | 主页必须能跳到新页面 | 用户感知不到新项目 |

**必须保留这些原版检查的全部逻辑**，不是只保留名字。

### 问题 2：没同步到 skill 仓库（source of truth）

`scripts/quality_gate.py` 在 github-trending 仓库只是个**调用方副本**，
真正的 source of truth 在 `/root/repos/skills/github-trending-monitor/`。
下次 cron 跑会用老版本。

## 你要做的事

### Step 1: 设计合并方案

合并后的 quality_gate.py 必须同时包含：

**原版 6 条**（来自 sha=4e15c311）：
1. 主页面文件存在 + 子页面目录存在
2. layout shell 锚点齐全
3. 禁用旧设计系统（B 系 + A 系）
4. 主页面 H3 ≥ 10
5. 深度阅读入口 ≥ 2
6. 子页面 layout shell 与主页面一致 + CSS vars 一致
7. index.html 含项目卡片

**你的 4 条新检查**（BATCH 1 加的）：
8. line_refs ≥ 50（保留）
9. code_wraps ≥ 6（主页）/ ≥ 3（子页）
10. bulk_line_piles = 0（禁止 ≥15 个连续 `<code>path:NN</code>` 分隔符堆叠）
11. low_explanation_refs 软警告

建议用 dataclass + 函数式检查链，避免面条代码。

### Step 2: 输出到 4 个位置

**位置 1（source of truth，git 仓库）：**
`/root/repos/skills/github-trending-monitor/scripts/quality_gate.py`
- 这个仓库会 git push 到远端

**位置 2（github-trending 调用方副本）：**
`/root/repos/github-trending/scripts/quality_gate.py`
- 这个仓库的工作副本，已经存在 BATCH 1 的版本

**位置 3（Hermes 本地副本，覆盖即可）：**
`/root/.hermes/skills/github-trending-monitor/scripts/quality_gate.py`
- `/root/.hermes/skills/` 不是 git 仓库（是 Hermes 同步层），直接覆盖文件

**位置 4（Hermes 本地副本，遗留 skill 名）：**
`/root/.hermes/skills/github-trending-page-template/scripts/quality_gate.py`
- 这个文件当前是 fd7acf 版本（比老 skill 还旧），也覆盖

**确保 4 份文件 sha256 完全一致**（位置 3 和 4 是 Hermes 副本，可能 mtime 不同，但 sha 必须一致）。

### Step 3: 更新 SKILL.md（位置 1）

`/root/repos/skills/github-trending-monitor/SKILL.md` 改：
- "质量硬约束"表格加新规则（code_wraps / bulk_line_piles / low_explanation）
- 在"重要约束"段加：
  - "禁止批量行号堆砌段（≥15 个连续 `<code>path:NN</code>` 分隔符）"
  - "每个 `path:NN` 引用前后 200 字符内必须有 ≥30 字符的中文/英文讲解（软警告）"
- 在"失败处理"段补 3 种新报错说明

`/root/.hermes/skills/github-trending-monitor/SKILL.md` 同步覆盖（同 Step 2 位置 3）。

### Step 4: 跑回归测试

```bash
# 1. qwen-agentworld（上一轮改过，应该 PASS）
python3 /root/repos/skills/github-trending-monitor/scripts/quality_gate.py qwen-agentworld --json

# 2. openclaw（code_wrap=0 高危页，应该 FAIL 验证新检查生效）
python3 /root/repos/skills/github-trending-monitor/scripts/quality_gate.py openclaw --json

# 3. kimi-code（code_wrap=0，应该 FAIL）
python3 /root/repos/skills/github-trending-monitor/scripts/quality_gate.py kimi-code --json

# 4. opentag（合规旧项目，应该 PASS）
python3 /root/repos/skills/github-trending-monitor/scripts/quality_gate.py opentag --json

# 5. browser-bc（合规，应该 PASS）
python3 /root/repos/skills/github-trending-monitor/scripts/quality_gate.py browser-bc --json
```

如果合规的旧项目（opentag / browser-bc）被误 FAIL，调整阈值并在汇报里写明。

### Step 5: commit + push 远端

**两个仓库分别操作：**

1. **`/root/repos/skills/` 仓库**（要 push 远端）：
   ```bash
   cd /root/repos/skills
   # 改两个文件
   git add github-trending-monitor/scripts/quality_gate.py
   git add github-trending-monitor/SKILL.md
   git status --short  # 确认只有这两个文件
   git commit -m "合并 quality_gate 新旧逻辑：保留原版 6 项护栏，新增防批量行号堆砌、强制真代码块、讲解密度三项约束"
   git push https://x-access-token:$GITHUB_PAT@github.com/classmatexiaoming96-ux/skills.git main
   ```
   push 前用 `git log` 确认远端没有新提交；如有冲突先 fetch/rebase。

2. **`/root/repos/github-trending/` 仓库**（**不要 push**）：
   - 复制合并后的 quality_gate.py 到 `scripts/quality_gate.py`
   - **不要 git add/commit**（BATCH 1 的 3 个页面改动也还没 commit，
     让效铭统一处理）

### Step 6: 验证 + 汇报

完成后输出：
- 4 份 quality_gate.py 的 sha256（必须一致）
- 5 个回归测试项目的 PASS/FAIL 结果
- skills 仓库 commit hash + push URL
- skills 仓库 git log -3
- 是否有阈值调整 + 调整理由

完成后输出 `BATCH 2 DONE` 作为 sentinel。

## 约束

- commit message 用业务语言（不要 P0/P1/P2 编号）
- 不要修改其他页面
- 不要修改 index.html
- 保留原版 6 条检查的**全部逻辑**（不是只保留名字）
- `/root/.hermes/skills/` 不是 git 仓库，直接覆盖文件即可
- skills 仓库 push 失败的话不要泄露 token，只报告 PAT 可能失效 / 权限不足 / 远端有冲突
