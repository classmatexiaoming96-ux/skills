# 任务：比较"分数逻辑" vs "门槛逻辑"的准确性

## 背景

你（BATCH 3）已经在 quality_gate.py 里实现了两套评估：
1. **门槛检查（硬门禁）**：每个维度独立检查，全通过就 PASS，任何一项不达标就 FAIL
2. **分数评分（100 分制）**：7 个维度加权，总分 A/B/C/D，但当前仅展示，不影响 PASS/FAIL

用户效铭想知道：**分数逻辑 vs 门槛逻辑，哪个更准确？**

## 你需要做的事

### Step 1: 全站扫描每个主页面，算出两套结果

对 /root/repos/github-trending/ 下所有主页面（.html），每个页面输出：

```
页面: opentag.html
  门槛检查: PASS / FAIL  【当前状态】
  FAIL 原因: 列表
  分数: 73.5 分 （C 级）
  分数明细:
    - code_wraps: 10.0/15 (8/4)
    - explanation_paragraphs: 5.0/15 (2/6)
    - explanation_ratio: 0.9/20 (3.2%)
    - h3_count: 5.0/5 (13/10)
    - deep_links: 5.0/5 (2/2)
    - layout: 5.0/5
    - index_card: 0/5
    - bulk_penalty: -30.0
    - low_explanation_penalty: -20.0
  总: 100 - (缺失项扣分) - penalty
  grade: C (73.5)
```

跑了之后把这个 markdown 写到 /tmp/score-vs-gate-comparison.md。

### Step 2: 写一个对比矩阵

| 页面 | 门槛结果 | 分数结果 | 分级 | 是否一致 | 不一致原因分析 |
|---|---|---|---|---|---|
| opentag.html | FAIL | C (73.5) | 门槛FAIL但不至于F | 分数更合理 | 有code_wrap但讲解不足 |
| openclaw.html | FAIL | F (XX) | 门槛FAIL+分数F | 一致 | 零代码块 |
| qwen-agentworld.html | PASS | B (XX) | 门槛PASS+分数B | 一致 | 全维度合格 |
| ... | ... | ... | ... | ... |

"是否一致" 只看：
- 门槛FAIL + 分数≤60（F级）→ 一致（都判不过）
- 门槛PASS + 分数≥60（D及以上）→ 一致（都判过）
- 门槛FAIL + 分数≥60 → **不一致**（门槛太严，分数觉得可以过）
- 门槛PASS + 分数≤60 → **不一致**（门槛太松，分数觉得不该过）

**最后列出：几个一致的 / 几个不一致的 / 不一致的具体原因**

### Step 3: 深入分析 3 类典型页面

1. **opentag 类——反典型**：门槛FAIL但分数C（有 code_wrap 但全堆砌+几乎不讲解）
   - 分数C 是不是太客气了？
   - 如果分数制在 100 分制下也让这种页面得 C 而不是 F，说明分数权重有问题？
2. **browser-bc 类——合规但指标差**：门槛FAIL（code_wraps=4 < 6），分数多少？
   - 如果分数觉得它可以过（≥60），说明分数比门槛更人性化？
3. **qwen-agentworld 类——真优质**：门槛PASS，分数A/B
   - 作为标杆，它各项多少分？

### Step 4: 给出结论

回答效铭的核心问题：**分数逻辑准确，还是门槛逻辑准确？**

- 如果一致率高（≥85%），说明两套本质上是一回事——分数只是门槛的可视化
- 如果不一致率集中在某一类页面，说明另一套更准
- 如果 openclaw 在分数制下竟然≥60 分，说明分数权重有问题

给出明确建议：
- 保持门槛检查作为 PASS/FAIL 的唯一仲裁者（现状）？
- 还是把分数制升级硬指标：总分 < 60 直接 FAIL 而不是只展示？
- 还是两套并存，各自负责不同环节？

### Step 5: 写报告到文件

把上面所有内容写入 /tmp/score-vs-gate-comparison.md（markdown 格式，中文）。

完成后输出 `COMPARISON DONE` 作为 sentinel。

## 约束

- 不要修改任何页面
- 不要修改 quality_gate.py
- 不要 git add/commit/push
- 只跑脚本 + 读文件 + 写报告